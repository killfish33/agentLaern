export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && . "$NVM_DIR/nvm.sh"

if command -v nvm >/dev/null 2>&1; then
  nvm use --silent >/dev/null 2>&1 || nvm use --silent 20.19.0 >/dev/null 2>&1
fi

npx lint-staged
